setwd("~/data-sets/Machine Learning A-Z/Part 5 - Association Rule Learning/Section 28 - Apriori")
# Data Preprocessisng
# install.packages("arules")
library(arules)
dataset = read.transactions('Market_Basket_Optimisation.csv',sep = ',',rm.duplicates = TRUE)
summary(dataset)
itemFrequencyPlot(dataset,topN = 50)
# Traing Apriori on Dataset
rules = apriori(dataset,parameter = list(support = 0.003, confidence = 0.4))
library(arules)
dataset = read.transactions('Market_Basket_Optimisation.csv',sep = ',',rm.duplicates = TRUE)
summary(dataset)
itemFrequencyPlot(dataset,topN = 50)
rules = apriori(dataset,parameter = list(support = 0.003, confidence = 0.4))
summary(rules)
inspect(rules[1:20])
inspect(sort(rules[1:20],by= 'lift'))
inspect(sort(rules[1:20],by= 'lift'))
rules = apriori(dataset,parameter = list(support = 0.003, confidence = 0.6))
inspect(sort(rules[1:20],by= 'lift'))
rules = apriori(dataset,parameter = list(support = 0.003, confidence = 0.6))
inspect(sort(rules[1:20],by= 'lift'))
inspect(sort(rules[:],by= 'lift'))
inspect(sort(rules[1:2],by= 'lift'))
rules = apriori(dataset,parameter = list(support = 0.003, confidence = 0.6))
inspect(sort(rules[1:12],by= 'lift'))
rules = apriori(dataset,parameter = list(support = 0.003, confidence = 0.7))
inspect(sort(rules[1:1,by= 'lift'))
inspect(sort(rules[1:1],by= 'lift'))
rules = apriori(dataset,parameter = list(support = 0.003, confidence = 0.6))
inspect(sort(rules[1:12],by= 'lift'))
rules = apriori(dataset,parameter = list(support = 0.0003, confidence = 0.6))
rules = apriori(dataset,parameter = list(support = 0.0003, confidence = 0.8))
rules = apriori(dataset,parameter = list(support = 0.003, confidence = 0.8))
rules = apriori(dataset,parameter = list(support = 0.003, confidence = 0.2))
inspect(sort(rules[1:1348],by= 'lift'))
View(dataset)
itemFrequencyPlot(dataset,topN = 50)
rules = apriori(dataset,parameter = list(support = 0.004, confidence = 0.2))
inspect(sort(rules[1:20],by= 'lift'))
